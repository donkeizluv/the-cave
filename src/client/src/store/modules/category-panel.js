import { CREATE, GET_ALL } from "../actions/category-panel/action-types";

const actions = {
  [CREATE]: () => {
    // create new cat
  },
  [GET_ALL]: () => {
    // create new cat
  }
};

export default {
  actions
};
